#include <iostream>

int main() {
    std::cout << "is this shi working?" << std::endl;
    return 0;
}

// To check is this even working just run two commands 
// g++ (file name as it is with extension) -o (name of the output file you want it to be)